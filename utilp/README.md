# utilp

`utilp` is a powerputty package to provide utilities and helpers that don't really fit anywhere else.

## Goals And Features

* Generic Map functions

### Generic Map Function

Map over a `Seq` (or `Seq2`) generically.
